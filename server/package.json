{
    "name": "algo-interview-llm-server",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "start": "bun run index.ts",
        "lint": "eslint --ext .ts .",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "bun-types": "latest",
        "eslint": "^8.49.0",
        "prettier": "^3.0.3"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
